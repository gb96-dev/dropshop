spring:
  datasource:
    url: jdbc:mysql://localhost:3306/nbcam?useSSL=false&serverTimezone=Asia/Seoul&allowPublicKeyRetrieval=true&characterEncoding=UTF-8
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: star816438*

  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: create-drop
    show-sql: false
    properties:
      hibernate:
        format_sql: true
        use_sql_comments: true

  h2:
    console:
      enabled: false

product:
  policy:
    deliveryInfo: "기본 배송 정책: 평일 1-2일 배송"
    refundPolicy: "기본 환불 정책: 수령 후 7일 이내 미개봉 상태에서만 가능"
  image-upload:
    bucket: drop-product-image-574070665489-ap-northeast-2-an
    region: ap-northeast-2
    key-prefix: products
    cdn-base-url: https://drop-product-image-574070665489-ap-northeast-2-an.s3.ap-northeast-2.amazonaws.com
    presigned-expiration-seconds: 300

jwt:
  secret: ${JWT_SECRET:local-jwt-secret-key-change-me}

aws:
  credentials:
    access-key: ${AWS_ACCESS_KEY}
    secret-key: ${AWS_SECRET_KEY}

cloud:
  aws:
    credentials:
      access-key: ${AWS_ACCESS_KEY}
      secret-key: ${AWS_SECRET_KEY}
    s3:
      bucket: drop-product-image-574070665489-ap-northeast-2-an

logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE